## Face OSINT + Crowd Intelligence Center
## Python 3.10 or 3.11 required — TensorFlow 2.16 does not support 3.12
## Install: pip install -r requirements.txt

# ── Web framework ───────────────────────────────────────────────────────────
flask
flask-cors
requests
python-dotenv

# ── HTML parsing ─────────────────────────────────────────────────────────────
beautifulsoup4
lxml

# ── Image & camera ───────────────────────────────────────────────────────────
opencv-python
numpy
Pillow

# ── Face recognition ─────────────────────────────────────────────────────────
# Downloads ~600 MB Facenet512 weights to data/models/ on first run.
deepface
tensorflow
tf-keras          # required by TensorFlow 2.16+

# ── Fuzzy string matching (entity resolution) ────────────────────────────────
# Falls back to a slower pure-Python implementation if absent.
rapidfuzz

# ── Crowd Intelligence Center (CIC) ─────────────────────────────────────────
# Core CIC features — required for the ⚡ CIC mode.
#   YOLOv8n: auto-downloads 6 MB model on first run; person detection at ~5 FPS on CPU.
#   anthropic: Claude API streaming operator assistant (set ANTHROPIC_API_KEY in .env).
ultralytics>=8.0
anthropic>=0.25

# ── Optional OSINT extras ─────────────────────────────────────────────────────
# Uncomment to enable; the system works without them.
# sherlock-project   # username OSINT — 300+ platforms (vs 125 built-in direct checks)
# socid-extractor    # social ID extraction from profile pages
# praw               # authenticated Reddit API (falls back to public JSON without it)

# ── Playwright (browser-based scrapers) ──────────────────────────────────────────────
# Adds ENGINE 6 (Google Lens) + ENGINE 7 (Bing Visual) to reverse_face.py
# and Layer 4 JS-profile verification to username.py. All free, no API keys.
# After pip install, run:  playwright install chromium
# On WSL2 also run:        playwright install-deps chromium
# playwright
